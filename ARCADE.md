# Pocket Worlds · 主页街机说明

原来的 5 种展示保留；新增 8 种原创场景、6 类每日谜题、找机器人彩蛋，以及一条会持续留下明信片的旅行线。主页每天只摆一台街机，谜题、答案和日志折叠显示，项目介绍不改动。

## 能看，也能动脑

| 原创卡带 | 每次重新生成的东西 |
| --- | --- |
| `neon-race` · 霓虹接力 | 四位选手的速度曲线、反超过程与终点次序；先猜赢家，再展开答案。 |
| `dungeon-crawl` · 发条地牢 | DFS 雕刻的迷宫与真实可达的宝箱路径；机器人沿路径行走。 |
| `pocket-garden` · 口袋花园 | 树高、树冠、花朵、萤火虫及虚构天气。无需浇水，不惩罚离开。 |
| `orbital-rescue` · 星际邮局 | 星野与轨道相位；三颗小卫星绕着小世界送信。 |
| `life-lab` · 生命实验室 | 28×9 有限网格、24 代真实 Conway B3/S23 演化，而不是预制随机闪烁。 |
| `sorting-race` · 排序大奖赛 | 同一副新洗的牌；冒泡与插入排序按相同比较节拍播放，先结束者等待。 |
| `midnight-metro` · 午夜小地铁 | 七个车站的站名、位置与环线；三辆车会停站。 |
| `deep-sea` · 深海来信 | 发光水母、气泡与潜艇漂游的海底场景。 |

每幅原创场景都藏着一只金色小机器人。彩蛋折叠区会给出相对图像的位置；赛车还会公布真正先到终点的选手。所有原创场景都有深浅两套主题，并响应减少动态效果的系统偏好。

这里明确区分两件事：SVG 是**自动播放的观赏场景**，不是键盘小游戏。每日谜题可以直接在 README 阅读、思考、展开提示和答案，但不提供输入框、自动判分或在线排行榜。

每日谜题每 6 个自然日形成一个洗牌袋，袋内六种题型各一次，跨袋不连续同类。一个新的安装可能从当前自然日袋的中间开始。题目不是有限的一组固定问答卡，而是按日期生成参数。

- 密码锁：0–5 不重复的三位密码；枚举全部 120 种密码验证唯一解。
- 取石子：给出合法的必胜走法；可能存在其他正确答案。
- 最省路线：4×4 权重图，只能右移、下移；最优能耗含起终点，允许等价最优路线。
- 熄灯：3×3 十字翻转；生成可解局面并穷举验证最少按键数。
- 24 点：四张牌，每张使用一次；用精确分数求解，允许中间分数。
- 骑士跳跃：4×4 棋盘内的最短路线；用 BFS 求解，可能有多条最短路。

## 明信片与收藏不是访问统计

这是一条**全体访客共享的虚构旅行线**。每个成功发布的新日期推进一站，不根据浏览、点击、提交数量或答题计分，也没有追踪脚本。

一卷 48 站，经过 8 个区域，每区 6 站。首卷的 48 件纪念品不重复；新卷继续旅行、保留已有收藏。每第 7 个旅行日保证出现稀有事件，其他日子也有小概率遇见。日志只保留最近 7 条，状态文件不会无限膨胀。

离开主页不会损失任何东西：只要每日发布正常，旅行仍会继续。如果自动发布缺席多天，恢复时只前进一步，不补出一堆待完成任务。手动换卡带和同日重跑均不会多领收藏。

## 刷新与启用

沿用原来的 `17 1 * * *`：计划每天 **01:17 UTC，即北京时间 09:17** 运行。日期也使用 UTC，维持旧流程的日期边界；不是按每位访客的本地午夜实时刷新。GitHub Actions 有可能延迟甚至漏掉计划任务，因此页面显示的是最后一次成功发布的日期，不假装已经刷新。

合并更新后，下一次每日工作流会自动使用新引擎。要马上看原创场景，在本仓库 **Actions → Daily profile arcade → Run workflow**，选择 `dungeon-crawl` 或其他新卡带，保持 `generate_all=false`。这不会额外推进同日的收藏。

本次代码更新不提交每天自动变化的 README、状态或图片，避免 PR 等待期间与每日机器人的提交发生冲突。第一次成功运行新版工作流时，图片、谜题与状态一起发布，不先放出不存在的图片链接。旧状态升级时会把 8 张新卡带优先加入剩余袋，不必等旧袋抽完。此迁移袋可能少于 13 张；之后完整轮换袋都是 13 张。手动强制选择不属于随机轮换保证范围。

## 本地运行与验证

新增引擎只用 Python 标准库，建议 Python 3.12；不需要新 API Key、付费服务、Pages 配置或额外 pip 包。原有五个外部生成器及其依赖保持原样，仍可能受上游服务影响。原创场景不读取贡献数据，不能把它们当成真实活动图。

在仓库根目录运行：

```bash
python -m unittest discover -s tests -v
python scripts/rotate_arcade.py --experience dungeon-crawl
```

第二条命令**只准备** `rotation-output/`，不会改写正在展示的 README 或状态。里面有 `README.next.md`、`arcade-state.next.json` 和原创 SVG。

预览另一天时，用独立的、尚不存在的状态文件和输出目录，避免试图倒转当前旅行日期：

```bash
python scripts/rotate_arcade.py --date 2026-10-01 --experience deep-sea --state preview-state.json --output-dir preview-output
```

不要创建 `preview-state.json`，或者确保它是你专门维护的预览存档；上面的命令不会创建/覆盖这个输入状态路径。预览所有原创图：

```bash
python scripts/rotate_arcade.py --experience dungeon-crawl --generation-mode all --output-dir preview-all
```

这里会本地生成 16 个原创 SVG（8×深浅）；完整的五个旧生成器仍由 Actions 运行，不会凭空在本地生成旧资源。

测试覆盖 300 个完整轮换袋、旧状态迁移、同日重试、144 个参数化谜题样例、100 张迷宫、100 副排序牌、365 日旅行、深浅 SVG、缺失资源/可执行 SVG 拒绝，以及选择模式/全量模式的发布串联。旧生成器的全量发布测试使用输入夹具，并非声称在本地运行了那些外部服务。

## 发布保护与取舍

选择与原创生成放在只读 `choose` 作业中；第三方生成器仍处于独立的只读作业。只有最后的 `publish` 拿 `contents: write`。不添加 Issue 写权限，不执行访客输入，不建立每次点击都触发 Actions 的入口。

发布器先验证所有必需文件、SVG 与元数据，再拷入工作区；Git 提交是远端整体发布边界。失败时不提交新 README 或新收藏。没有强推、自动解决冲突或自动覆盖最新分支；遇到并发修改导致推送失败，应重新运行。新日期后的运行失败会保留旧展示，而不是伪装成更新成功。

原创 SVG 使用 CSS 动画和本地生成的数据，不包含脚本、外链图片或字体下载。仓库图片链接带日期参数帮助区分缓存，但 GitHub 图片代理的刷新延迟仍不完全可控。新版会在自动化代码 PR 中运行只读测试，在每日生成前也先跑测试。

GitHub README 会清理 JavaScript 等主动内容；真正可用方向键操作的游戏需要另外的网页承载，这一版不擅自给你开 Pages 或改变仓库设置。通过 Issue 下棋是另一种可行路线，但每步创建 Issue 的交互不适合这里希望实现的轻量每日小惊喜。

参考（机制说明，原创代码未复制这些项目的实现）：

- GitHub Markdown 清理流程：https://github.com/github/markup
- GitHub Actions 计划任务、默认分支及不活跃限制：https://docs.github.com/en/actions/reference/workflows-and-actions/events-that-trigger-workflows#schedule
- Issue 驱动的社区国际象棋实例：https://github.com/timburgan/timburgan

如果公有仓库长期没有活动，GitHub 的计划任务可能被自动停用；在 Actions 中重新启用即可。不要依靠这个小玩具做需要精确时刻的提醒。
